// This should be avoided in large projects. Since this is a task for me, I have decided to declare this here.

type Todo = {
  id: string;
  title: string;
  createdAt: string;
  completed?: boolean;
  updatedAt?: string;
};
